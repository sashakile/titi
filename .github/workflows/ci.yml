name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore dependencies
        # RestoreLockedMode is enabled by Directory.Build.props when CI=true
        # Skip if no projects exist yet
        run: |
          if find src test -maxdepth 3 -name "*.csproj" 2>/dev/null | grep -q .; then
            dotnet restore src/titi/titi.csproj
          else
            echo "No .csproj files found, skipping restore"
          fi
        env:
          CI: true

      - name: Build
        run: |
          if find src test -maxdepth 3 -name "*.csproj" 2>/dev/null | grep -q .; then
            dotnet build src/titi/titi.csproj --no-restore --configuration Release
          else
            echo "No .csproj files found, skipping build"
          fi

      - name: Test
        run: |
          dotnet test src/titi/titi.csproj --no-build --configuration Release --collect:"XPlat Code Coverage" || echo "No test projects found, skipping"

      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          files: '**/coverage.cobertura.xml'
          fail_ci_if_error: false
